name: "GVL Commandline Utilities"
version: "{{ gvl_cmdlineutils_version }}"
build_date:  "{{ gvl_cmdlineutils_build_date }}"
rstudio_version: "{{ rstudio_version }}"
jupyterhub_commit: "{{ jupyterhub_commit }}"
configurable_http_proxy_version: "{{ configurable_http_proxy_version }}"
ipython_notebook_version: "{{ ipython_notebook_version }}"
matplotlib_version: "{{ matplotlib_version }}"
scipy_version: "{{ scipy_version }}"
pandas_version: "{{ pandas_version }}"