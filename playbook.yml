
# First attempt at ansible playbook version of these scripts

# Usage: ansible-playbook ansible_test.yml

- hosts: localhost  # prob unneeded since local
  connection: local
  sudo: yes
  sudo_user: root
  roles:
      - gvl.commandline-utilities
